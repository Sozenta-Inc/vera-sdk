[package]
name = "demo-echo"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
vera-client = { path = "../../sdk" }
tokio = { version = "1", features = ["rt-multi-thread", "macros"] }
